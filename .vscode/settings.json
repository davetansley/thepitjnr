{
    "medius.runonsave": {
        "commands": [
            {
                "match": "src/.*.lua",
                "isAsync": true,
                "cmd": "cat ${workspaceRoot}/src/*.lua ${workspaceRoot}/src/*/*.lua > ${workspaceRoot}/main.lua"
            }			
        ]
    }
}